// Tempo para o próximo spawn (30 segundos assumindo 60 fps)
spawn_interval = 60 * 30; 

// 5 segundos
timer = 400; 

// Inicializa a variável global antes de qualquer coisa acontecer!
global.kobolds_spawned = 0;